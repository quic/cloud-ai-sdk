# Health and Resource monitoring
Developers can monitor the health/resources of Cloud AI hardware through CLI (command line inferface) utility, Python and C++ APIs. <br> <br>
**Health** refers to the functional state of the Cloud AI card. Its a binary state - Good or Error. A card in error state needs to go through the recovery steps. <br>
**Resources** refers to compute/memory/IO resouces.   

## CLI 
`qaic-util` is a CLI tool developers can use to query
- card health
- firmware version
- compute/memory/IO resources available vs in-use
- card power and temperature 
- status of certain device capabilities like ECC etc

`qaic-util` presents information in 2 different formats:
- vertical format where cards are queried once and the parameters are listed one per line. 
```
/opt/qti-aic/tools/qaic-util -q 
```
- tabular format where certain parameters (compute, IO, power, temperature etc) are listed in a tabular format, refreshed every 'n' seconds (user input)
```
/opt/qti-aic/tools/qaic-util -q -t 1 
```

## C++ APIs

## Python APIs
