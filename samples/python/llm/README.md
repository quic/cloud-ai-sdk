```python
usage: example.py [-h] [--model MODEL]

python example.py --model albert-base-v2

optional arguments:
  -h, --help     show this help message and exit
  --model MODEL  select one of the LLMs 'distilbert-base-uncased' 'bert-base-uncased' 'bert-large-uncased' 'bert-base-cased' 'albert-base-v2' 'xlm-roberta-large'
```