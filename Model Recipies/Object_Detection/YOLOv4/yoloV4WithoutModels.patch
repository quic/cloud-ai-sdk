diff --git a/detect.py b/detect.py
index 42e2576..73ea248 100644
--- a/detect.py
+++ b/detect.py
@@ -4,9 +4,11 @@ from models import *  # set ONNX_EXPORT in models.py
 from utils.datasets import *
 from utils.utils import *
 
+ONNX_EXPORT = True
 
 def detect(save_img=False):
-    imgsz = (320, 192) if ONNX_EXPORT else opt.img_size  # (320, 192) or (416, 256) or (608, 352) for (height, width)
+    imgsz = (416, 416) if ONNX_EXPORT else opt.img_size  # (320, 192) or (416, 256) or (608, 352) for (height, width)
+    imgsz = tuple(opt.export_input_size) if ONNX_EXPORT and opt.export_input_size else imgsz
     out, source, weights, half, view_img, save_txt = opt.output, opt.source, opt.weights, opt.half, opt.view_img, opt.save_txt
     webcam = source == '0' or source.startswith('rtsp') or source.startswith('http') or source.endswith('.txt')
 
@@ -42,16 +44,25 @@ def detect(save_img=False):
     # Export mode
     if ONNX_EXPORT:
         model.fuse()
+        h, w = imgsz
         img = torch.zeros((1, 3) + imgsz)  # (1, 3, 320, 192)
-        f = opt.weights.replace(opt.weights.split('.')[-1], 'onnx')  # *.onnx filename
+        traced = torch.jit.trace(model, (img), optimize=False)
+        traced.save(f"yolov4_{h}_{w}_without_ABP_NMS.pt")
+        # f = opt.weights.replace(opt.weights.split('.')[-1], 'onnx')  # *.onnx filename
+        f_output = f'yolov4_{h}_{w}_without_ABP_NMS.onnx'
+        print("================================Pytorch EXPORT=======================================")
+        f = f'yolov4_{h}_{w}_without_ABP_NMS.onnx'
         torch.onnx.export(model, img, f, verbose=False, opset_version=11,
-                          input_names=['images'], output_names=['classes', 'boxes'])
+                input_names = ['images'])
 
         # Validate exported model
         import onnx
         model = onnx.load(f)  # Load the ONNX model
         onnx.checker.check_model(model)  # Check that the IR is well formed
-        print(onnx.helper.printable_graph(model.graph))  # Print a human readable representation of the graph
+        shapeModel = onnx.shape_inference.infer_shapes(model)
+        onnx.save(shapeModel, f_output)
+        print(f"================================ONNX {f_output} EXPORT COMPLETED=======================================")
+        # print(onnx.helper.printable_graph(model.graph))  # Print a human readable representation of the graph
         return
 
     # Half precision
@@ -172,6 +183,7 @@ if __name__ == '__main__':
     parser.add_argument('--source', type=str, default='data/samples', help='source')  # input file/folder, 0 for webcam
     parser.add_argument('--output', type=str, default='output', help='output folder')  # output folder
     parser.add_argument('--img-size', type=int, default=512, help='inference size (pixels)')
+    parser.add_argument('--export-input-size', nargs='+', type=int, help='inference size (pixels)')
     parser.add_argument('--conf-thres', type=float, default=0.3, help='object confidence threshold')
     parser.add_argument('--iou-thres', type=float, default=0.6, help='IOU threshold for NMS')
     parser.add_argument('--fourcc', type=str, default='mp4v', help='output video codec (verify ffmpeg support)')
diff --git a/models.py b/models.py
index 54742b8..9962c23 100755
--- a/models.py
+++ b/models.py
@@ -2,7 +2,7 @@ from utils.google_utils import *
 from utils.layers import *
 from utils.parse_config import *
 
-ONNX_EXPORT = False
+ONNX_EXPORT = True
 
 
 def create_modules(module_defs, img_size, cfg):
@@ -14,6 +14,9 @@ def create_modules(module_defs, img_size, cfg):
     module_list = nn.ModuleList()
     routs = []  # list of layers which rout to deeper layers
     yolo_index = -1
+    # To avoid dimensions issues when exporting to ONNX
+    # https://github.com/ultralytics/yolov3/issues/1169#issuecomment-650678238
+    upsample_index = 0
 
     for i, mdef in enumerate(module_defs):
         modules = nn.Sequential()
@@ -70,7 +73,9 @@ def create_modules(module_defs, img_size, cfg):
 
         elif mdef['type'] == 'upsample':
             if ONNX_EXPORT:  # explicitly state size, avoid scale_factor
-                g = (yolo_index + 1) * 2 / 32  # gain
+                # g = (yolo_index + 1) * 2 / 32  # gain
+                g = (upsample_index + 1) * 2 / 32  # gain
+                upsample_index += 1
                 modules = nn.Upsample(size=tuple(int(x * g) for x in img_size))  # img_size = (320, 192)
             else:
                 modules = nn.Upsample(scale_factor=mdef['stride'])
@@ -92,6 +97,9 @@ def create_modules(module_defs, img_size, cfg):
 
         elif mdef['type'] == 'yolo':
             yolo_index += 1
+            # stride = [8, 16, 32]  # P5, P4, P3 strides
+            # if any(x in cfg for x in ['yolov4-tiny']):  # stride order reversed
+            #    stride = [32, 16, 8]
             stride = [32, 16, 8]  # P5, P4, P3 strides
             if any(x in cfg for x in ['panet', 'yolov4', 'cd53']):  # stride order reversed
                 stride = list(reversed(stride))
@@ -168,7 +176,7 @@ class YOLOLayer(nn.Module):
     def forward(self, p, out):
         ASFF = False  # https://arxiv.org/abs/1911.09516
         if ASFF:
-            i, n = self.index, self.nl  # index in layers, number of layers
+            i, n = self.index,   self.nl  # index in layers, number of layers
             p = out[self.layers[i]]
             bs, _, ny, nx = p.shape  # bs, 255, 13, 13
             if (self.nx, self.ny) != (nx, ny):
@@ -205,13 +213,13 @@ class YOLOLayer(nn.Module):
             ng = 1. / self.ng.repeat(m, 1)
             grid = self.grid.repeat(1, self.na, 1, 1, 1).view(m, 2)
             anchor_wh = self.anchor_wh.repeat(1, 1, self.nx, self.ny, 1).view(m, 2) * ng
-
-            p = p.view(m, self.no)
-            xy = torch.sigmoid(p[:, 0:2]) + grid  # x, y
-            wh = torch.exp(p[:, 2:4]) * anchor_wh  # width, height
-            p_cls = torch.sigmoid(p[:, 4:5]) if self.nc == 1 else \
-                torch.sigmoid(p[:, 5:self.no]) * torch.sigmoid(p[:, 4:5])  # conf
-            return p_cls, xy * ng, wh
+            return p
+            # p = p.view(m, self.no)
+            # xy = torch.sigmoid(p[:, 0:2]) + grid  # x, y
+            # wh = torch.exp(p[:, 2:4]) * anchor_wh  # width, height
+            # p_cls = torch.sigmoid(p[:, 4:5]) if self.nc == 1 else \
+            #     torch.sigmoid(p[:, 5:self.no]) * torch.sigmoid(p[:, 4:5])  # conf
+            # return p_cls, xy * ng, wh
 
         else:  # inference
             io = p.clone()  # inference output
@@ -305,8 +313,10 @@ class Darknet(nn.Module):
         if self.training:  # train
             return yolo_out
         elif ONNX_EXPORT:  # export
-            x = [torch.cat(x, 0) for x in zip(*yolo_out)]
-            return x[0], torch.cat(x[1:3], 1)  # scores, boxes: 3780x80, 3780x4
+            # print(yolo_out[0].shape, yolo_out[1].shape, yolo_out[2].shape)
+            return yolo_out[0], yolo_out[1], yolo_out[2]
+            # x = [torch.cat(x, 0) for x in zip(*yolo_out)]
+            # return x[0], torch.cat(x[1:3], 1)  # scores, boxes: 3780x80, 3780x4
         else:  # inference or test
             x, p = zip(*yolo_out)  # inference output, training output
             x = torch.cat(x, 1)  # cat yolo outputs
